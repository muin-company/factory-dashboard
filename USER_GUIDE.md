# Factory Dashboard V2 — User Guide

실시간으로 AI 에이전트의 API 비용, 토큰 사용량, 구독 활용률을 모니터링하는 대시보드의 사용 가이드입니다.

---

## 목차

1. [시작하기](#시작하기)
2. [대시보드 접속](#대시보드-접속)
3. [화면 구성](#화면-구성)
4. [기간 필터](#기간-필터)
5. [지표 해석 가이드](#지표-해석-가이드)
6. [차트 읽기](#차트-읽기)
7. [활성 세션 모니터](#활성-세션-모니터)
8. [자주 묻는 질문](#자주-묻는-질문)
9. [문제 해결](#문제-해결)

---

## 시작하기

### 사전 요구사항

- Python 3.10 이상
- [OpenClaw](https://openclaw.com) 설치 및 하나 이상의 에이전트 실행 이력
- `~/.openclaw/agents/` 디렉토리에 세션 JSONL 파일 존재

### 설치 & 실행

```bash
# 1. 저장소 클론
git clone https://github.com/muin-company/factory-dashboard.git
cd factory-dashboard

# 2. 가상환경 생성 및 활성화
python3 -m venv venv
source venv/bin/activate

# 3. 의존성 설치
pip install -r requirements.txt

# 4. 서버 실행
python app.py
```

서버가 **http://localhost:5051** 에서 시작됩니다.

### 종료

터미널에서 `Ctrl+C`로 서버를 종료합니다.

---

## 대시보드 접속

브라우저에서 `http://localhost:5051`을 열면 대시보드가 바로 표시됩니다.

- **자동 새로고침**: 30초마다 데이터가 자동으로 갱신됩니다.
- **반응형 레이아웃**: 모바일, 태블릿, 데스크톱 모두 대응합니다.
- **별도 로그인 없음**: 로컬 전용 도구이므로 인증이 필요하지 않습니다.

---

## 화면 구성

대시보드는 위에서 아래로 다음 섹션들로 구성됩니다:

### 1. Summary Cards (상단 요약)

| 카드 | 의미 |
|------|------|
| **월 구독료** | 현재 구독 중인 AI 서비스의 총 월 비용 |
| **API 환산 가치** | 같은 사용량을 종량제(pay-per-use)로 지불했을 때의 예상 비용 |
| **총 토큰** | 선택 기간 동안 소비한 총 토큰 수 |
| **활성 일수** | 에이전트가 실제로 작동한 날의 수 |

> 💡 **API 환산 가치 > 월 구독료**이면 구독이 이득입니다.

### 2. Subscription Breakdown (구독 상세)

각 구독 서비스(Anthropic MAX, OpenAI Plus, Google AI Pro 등)별로:

- **월 요금** — 실제 지불하는 금액
- **API 환산 비용** — 해당 구독으로 사용한 만큼을 종량제로 냈을 때의 비용
- **절감액** — API 환산 비용 − 월 요금 (양수면 이득)
- **활용률(%)** — API 환산 비용 ÷ 월 요금 × 100

### 3. Utilization Bar (활용률 게이지)

전체 구독 대비 API 환산 가치를 게이지 바로 시각화합니다.

| 활용률 | 해석 |
|--------|------|
| **< 100%** | 🔴 구독 미활용 — 더 적극적으로 활용하거나 플랜 하향 검토 |
| **100–200%** | 🟡 적정 활용 — 구독 비용 이상의 가치를 얻고 있음 |
| **200–300%** | 🟢 높은 활용 — 구독 효과가 큼 |
| **> 300%** | 🔵 극한 활용 — 구독 없이는 3배 이상 비용 발생 |

### 4. Agent Breakdown (에이전트별 분석)

에이전트별로 다음을 확인할 수 있습니다:

- 소비한 총 비용 (API 환산)
- 총 토큰 (입력/출력/캐시 구분)
- 주로 사용하는 모델

### 5. Daily Charts (일별 차트 4종)

| 차트 | X축 | Y축 | 시각화 |
|------|-----|-----|--------|
| 비용 by Agent | 날짜 | USD | 에이전트별 스택 바 + 누적 라인 |
| 비용 by Model | 날짜 | USD | 모델별 스택 바 + 누적 라인 |
| 토큰 by Agent | 날짜 | 토큰 수 | 에이전트별 스택 바 |
| 토큰 by Model | 날짜 | 토큰 수 | 모델별 스택 바 |

> 💡 차트 범례를 클릭하면 특정 에이전트/모델을 숨기거나 표시할 수 있습니다.

### 6. Model Usage Table (모델 사용 테이블)

각 모델에 대해:

- API 환산 비용
- 토큰 사용량 (입력/출력)
- 요금제 유형 (구독 / 종량제)
- 사용 에이전트 목록

### 7. Agent × Model Matrix (교차 분석)

행: 에이전트, 열: 모델의 교차 테이블로, 어떤 에이전트가 어떤 모델을 얼마나 쓰는지 한눈에 파악합니다. 비용이 높은 셀이 강조 표시됩니다.

### 8. Active Sessions (활성 세션)

현재 실행 중인 에이전트 세션을 표시합니다:

- 세션 ID 및 타입 (main / subagent / cron)
- 사용 모델
- 상태 (active / idle)

---

## 기간 필터

대시보드 상단에 기간 필터가 있습니다:

| 프리셋 | 설명 |
|--------|------|
| **이번 주** | 월요일부터 현재까지 |
| **이번 달** | 1일부터 현재까지 |
| **7일** | 최근 7일 |
| **14일** | 최근 14일 |
| **30일** | 최근 30일 |
| **전체** | 모든 데이터 |
| **커스텀** | 시작/종료 날짜 직접 선택 |

기간을 변경하면 모든 카드, 차트, 테이블이 즉시 업데이트됩니다.

---

## 지표 해석 가이드

### 비용은 어떻게 계산되나요?

대시보드는 **실제로 지불하는 비용이 아닌**, 동일 사용량을 종량제 API로 환산한 비용을 보여줍니다.

```
API 환산 비용 = (입력 토큰 × 입력 단가) + (출력 토큰 × 출력 단가) + (캐시 읽기 × 캐시 단가)
```

가격은 `app.py`의 `PRICING` 딕셔너리에서 모델별로 정의되어 있으며, [LiteLLM 가격표](https://github.com/BerriAI/litellm/blob/main/model_prices_and_context_window.json)를 기반으로 합니다.

### 핵심 수치 읽기

| 보고 싶은 것 | 확인할 곳 |
|--------------|-----------|
| "이번 달 구독이 남는 장사인가?" | Summary Cards → API 환산 가치 vs 월 구독료 |
| "어떤 에이전트가 가장 비싸?" | Agent Breakdown 또는 일별 비용 by Agent |
| "어떤 모델이 가성비 좋은가?" | Model Usage Table → 비용 대비 토큰 수 |
| "구독을 업그레이드해야 하나?" | Subscription Breakdown → 활용률 |
| "비용 추이가 어떻게 변하나?" | Daily Charts → 누적 라인 기울기 |

### 활용률 기반 의사결정

- **활용률 < 50%**: 구독 다운그레이드 또는 더 적극적 활용 고려
- **활용률 100-200%**: 현재 플랜이 적절
- **활용률 > 300%**: 현재 플랜이 매우 효율적, 유지 권장
- **특정 구독만 활용률 낮음**: 해당 모델 사용 빈도 재검토

---

## 차트 읽기

### Stacked Bar Chart (스택 바)

- 각 막대의 색상은 에이전트 또는 모델을 나타냅니다.
- 막대의 전체 높이 = 해당 날짜의 총 비용/토큰
- 각 색상 영역의 크기 = 해당 에이전트/모델의 기여분

### Cumulative Line (누적 라인)

- 일별 비용의 누적 합계를 보여줍니다.
- 기울기가 가파르면 → 비용 증가 속도가 빠름
- 기울기가 완만하면 → 비용 증가가 안정적
- 꺾이는 지점 → 사용 패턴이 변한 시점

---

## 활성 세션 모니터

Active Sessions 섹션에서 현재 실행 중인 세션을 실시간으로 확인합니다.

### 세션 타입

| 타입 | 설명 |
|------|------|
| **main** | 메인 에이전트 세션 (사용자와 직접 대화) |
| **subagent** | 서브에이전트 (메인이 생성한 하위 작업자) |
| **cron** | 정기 실행 세션 (스케줄 기반) |

### 상태

- **active**: 현재 작동 중
- **idle**: 대기 상태

---

## 자주 묻는 질문

### Q: "API 환산 가치"는 실제 청구 금액인가요?

**아닙니다.** 동일 사용량을 구독 없이 종량제로 사용했을 경우의 추정 비용입니다. 실제 비용은 구독료(고정)입니다. 이 수치의 목적은 구독이 얼마나 효율적인지를 보여주는 것입니다.

### Q: 캐시 토큰은 뭔가요?

Anthropic과 일부 모델은 이전 대화를 캐싱하여 반복 사용 시 비용을 절감합니다. `cacheRead`는 캐시에서 읽은 토큰, `cacheWrite`는 캐시에 저장한 토큰입니다. 캐시 토큰은 일반 토큰보다 10-90% 저렴합니다.

### Q: 새 모델이 추가되면 어떻게 하나요?

`app.py`의 `PRICING` 딕셔너리에 모델 이름과 가격을 추가하세요. 자세한 방법은 [CONTRIBUTING.md](CONTRIBUTING.md)를 참고하세요.

### Q: 데이터는 어디에 저장되나요?

별도 데이터베이스는 없습니다. 대시보드는 `~/.openclaw/agents/*/sessions/*.jsonl` 파일을 직접 읽어서 실시간으로 계산합니다.

### Q: 원격에서 접속할 수 있나요?

기본 설정은 localhost 전용입니다. 원격 접속이 필요하면 `app.py`에서 호스트 설정을 변경하거나, SSH 터널을 사용하세요:

```bash
# 원격 머신에서 SSH 터널
ssh -L 5051:localhost:5051 user@host
```

### Q: 자동 시작하려면?

macOS의 경우 `launchd` plist를 생성하거나, cron에 등록합니다:

```bash
# crontab에 추가 (재부팅 시 자동 시작)
@reboot cd ~/factory-dashboard-v2 && source venv/bin/activate && python app.py &
```

---

## 문제 해결

### 대시보드가 빈 화면

- `~/.openclaw/agents/` 디렉토리에 세션 파일이 있는지 확인
- OpenClaw 에이전트가 한 번이라도 실행된 적이 있어야 합니다

### 비용이 0으로 표시됨

- 사용 중인 모델이 `app.py`의 `PRICING`에 정의되어 있는지 확인
- 정의되지 않은 모델은 기본 가격(`default`)으로 계산됩니다

### 차트가 렌더링되지 않음

- 브라우저 콘솔(F12)에서 에러를 확인하세요
- Chart.js가 CDN에서 로드되므로 인터넷 연결이 필요합니다

### 서버 시작 실패

```bash
# 포트 충돌 확인
lsof -i :5051

# 가상환경이 활성화되었는지 확인
which python  # → .../venv/bin/python 이어야 함

# 의존성 재설치
pip install -r requirements.txt
```

---

## 다음 단계

- **README.md** — 프로젝트 개요 및 API 문서
- **CONTRIBUTING.md** — 기여 가이드, 새 모델/차트 추가 방법
- **GitHub Issues** — 버그 리포트 및 기능 요청

---

*Factory Dashboard V2 — "일하는 AI, 누리는 인간" 🏭*
